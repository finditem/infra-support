import { Icon } from "@/components";

const LogHeader = () => {
  return (
    <section>
      <Icon height={26} name="errorLog" width={24} />
      <h1>장애/에러 로그</h1>
    </section>
  );
};

export default LogHeader;
