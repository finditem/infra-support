---
name: plan-page
description: monitor-web의 특정 페이지(src/pages/{PageName}/) 관련 작업을 요청받았을 때, 구현을 시작하기 전에 반드시 이 스킬을 먼저 실행한다. apps/monitor-web/docs/{PageName}/plan.md를 확인/생성하고, 작업 항목을 todo 체크리스트로 기록한 뒤 진행 상황을 갱신한다. 공통 컴포넌트, 전역 설정, monitor-server 등 특정 페이지에 국한되지 않는 작업에는 사용하지 않는다.
---

# plan-page

monitor-web 페이지별 작업 계획을 `apps/monitor-web/docs/{PageName}/plan.md`에 todo 체크리스트로 기록하고, 작업 진행에 따라 갱신한다.

## 대상 판단

요청이 `apps/monitor-web/src/pages/{PageName}/` 하위(또는 해당 페이지의 `_components`/`_hooks`/`_types`/`_utils`)를 다루면 대상 페이지로 본다. `PageName`은 `src/pages/` 하위 디렉토리명과 정확히 일치시킨다 (예: `Dashboard`, `ApiDetail`).

여러 페이지에 걸친 요청이면 각 페이지의 plan.md를 각각 갱신한다. 페이지에 국한되지 않는 작업(공통 컴포넌트, 전역 설정, 다른 앱)이면 이 스킬을 사용하지 않는다.

## 절차

1. **시작 전**: `apps/monitor-web/docs/{PageName}/plan.md`를 읽는다.
   - 파일이 없으면 새로 만든다: `# {PageName} 작업 계획` 제목 아래, 이번 요청의 작업 항목을 `- [ ] ...` 체크리스트로 정리해 작성한다.
   - 파일이 이미 있으면(제목만 있는 빈 파일 포함) 기존 내용을 그대로 두고, 이번 요청에 해당하는 새 작업 항목을 이어서 추가한다. 기존 완료(`- [x]`) 항목은 수정하지 않는다.
   - 작업 항목은 실제로 변경할 코드 단위로 구체적으로 쪼갠다 (예: "DetailHeader 컴포넌트 접근성 개선", "에러 상태 토스트 연결"). "작업하기"처럼 뭉뚱그리지 않는다.

2. **작업 진행 중**: 항목을 하나 마칠 때마다 즉시 해당 줄을 `- [x]`로 갱신한다. 여러 항목을 몰아서 한 번에 갱신하지 않는다.

3. **응답을 마치기 전**: plan.md의 상태가 실제 완료 여부와 일치하는지 다시 확인하고 저장한다. 이번 요청 범위에서 착수하지 못한 항목은 `- [ ]`로 남겨 다음 세션이 이어갈 수 있게 한다.

## 주의

- plan.md는 문서일 뿐이다. 코드 변경 없이 plan.md만 갱신하고 끝내지 않는다.
- 이 스킬은 문서화 절차만 다룬다. 빌드/린트/타입체크/테스트 실행 여부는 루트 CLAUDE.md의 표준 작업 흐름을 따로 따른다 (응답을 마치기 전 사용자에게 확인).
- git add/commit 대상에 plan.md를 포함할지는 일반 커밋 규칙(이번 응답에서 Claude가 건드린 파일만 add)을 그대로 따른다.
